# render.yaml
services:
  - type: web
    name: rag-server
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
    healthCheckPath: /api/health
    autoDeploy: true